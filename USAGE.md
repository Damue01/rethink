# ReThink 使用文档

## 简介

ReThink 是一个基于多 LLM 的方案思辨工具，帮助你从多个维度发现方案中的盲点与风险。

---

## 启动

双击文件夹中的 **ReThink.exe** 即可运行。

---

## 首次使用：配置模型

首次打开后需要先配置 AI 模型，否则无法使用任何功能。

1. 点击左侧导航栏底部的 **设置**（齿轮图标）
2. 进入 **模型管理**
3. 点击「添加 Provider」，填入以下信息：
   - **名称**：随意取名（如 "OpenAI"、"DeepSeek"）
   - **Base URL**：模型 API 地址（如 `https://api.openai.com/v1`）
   - **API Key**：你的 API 密钥
4. 点击「获取模型列表」选择模型，或手动输入模型名称
5. 点击「测试连接」确认配置可用
6. 返回首页开始使用

> 支持任何 OpenAI 兼容 API（OpenAI、DeepSeek、本地 Ollama 等）。可以添加多个 Provider。

---

## 四种使用模式

### 自由对话

最基础的模式。像普通 AI 对话一样使用，额外支持：
- 选择**方法论**引导模型用特定思维框架分析问题
- 加载 **Skill** 知识片段为模型补充领域知识
- **拖拽上传**文本文件作为对话上下文（最多 5 个，单文件 ≤ 400KB）

### 多模型对比

同一个问题同时发给多个模型，并排查看回答差异。使用前需要配置至少 2 个模型。

### 多智能体辩论

多个 AI 角色（批判者、支持者、技术专家、魔鬼代言人）围绕你的方案展开结构化辩论，自动轮流发言。

### 苏格拉底审查（开发中）

通过连续追问发现隐含假设和逻辑漏洞。

---

## 其他设置（可选）

| 设置项 | 说明 |
|--------|------|
| **方法论** | 管理审查方法论模板（苏格拉底提问、Pre-Mortem、红队分析等），可自定义增删 |
| **Skill** | 管理可注入对话的知识片段，支持分类和编辑 |
| **角色模板** | 自定义辩论角色的提示词 |
| **MCP** | 连接外部工具服务器，扩展模型的工具调用能力（网页抓取、文档检索等） |

---

## 注意事项

- 所有数据（配置、对话历史）保存在本地浏览器存储中，**清除浏览器数据会丢失**
- 内置了网页抓取、Bing 搜索、本地文件读取三个基础工具，无需额外配置即可使用
